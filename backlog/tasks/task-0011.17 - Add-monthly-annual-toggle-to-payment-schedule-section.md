---
id: TASK-0011.17
title: Add monthly/annual toggle to payment schedule section
status: Done
assignee:
  - '@kevin'
created_date: '2026-08-18 22:26'
updated_date: '2026-08-18 22:42'
labels: []
dependencies: []
parent_task_id: TASK-0011
ordinal: 35000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
buildAmortisationSchedule currently returns only a flat monthly ScheduleRow[]. Extend it to also produce an annual breakdown (each year's interest/principal summed from that year's monthly rows, balance taken as the balance at the end of the year), and let the payment schedule accordion switch between the two views via a toggle button so users can see either a detailed monthly schedule or a condensed yearly one.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 buildAmortisationSchedule (or a new function built on it) returns both a monthly row list and an annual row list for a given schedule
- [x] #2 Each annual row's interest and principal equal the sum of that year's monthly interest/principal, and its balance equals the balance after the year's final payment
- [x] #3 PaymentScheduleSection renders a toggle button labelled 'See Annual' when monthly rows are shown and 'See Monthly' when annual rows are shown, switching the displayed rows on click
- [x] #4 The toggle button is only rendered inside the open collapsible content, never visible when the accordion is closed
- [x] #5 Single, During-deal, and After-deal cards all pass both monthly and annual rows through to PaymentScheduleSection and the toggle works correctly on each
- [x] #6 Existing vitest suite and typecheck pass with no regressions
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. types.ts: add AmortisationSchedule { monthly: ScheduleRow[]; annual: ScheduleRow[] }.
2. calculations.ts: rewrite buildAmortisationSchedule to a single pass that builds monthly rows as today, accumulates yearInterest/yearPrincipal raw totals, and pushes an annual row (period 'Year N', summed interest/principal, balance = balance at that year's last processed month) every 12 months and on the final month if it lands mid-year. Return { monthly, annual }.
3. copy.json: add paymentScheduleSection.columnYear ('Year'), paymentScheduleSection.showAnnualButton ('See Annual'), paymentScheduleSection.showMonthlyButton ('See Monthly').
4. payment-schedule-section.tsx: change prop from rows: ScheduleRow[] to schedule: AmortisationSchedule; add useState<'monthly'|'annual'> view mode; render a toggle button inside CollapsibleContent (above the table) showing the copy for the *other* mode and switching on click; swap the Month/Year column header and body rows based on view mode.
5. single-result-card.tsx, during-deal-card.tsx, after-deal-card.tsx: change paymentSchedule prop type to AmortisationSchedule; pass schedule={paymentSchedule} instead of rows={paymentSchedule} into PaymentScheduleSection.
6. view-model.ts: no signature changes needed since buildAmortisationSchedule's return type change flows through automatically; after-deal's postDealMonths<=0 zero-card needs paymentSchedule: { monthly: [], annual: [] } instead of [].
7. Typecheck + run vitest suite; manually verify in the running app that the toggle switches rows and stays hidden while collapsed.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verified: npx tsc --noEmit clean; npx vitest run resources/js/pages/mortgage-calculator-page 4/4 passing; eslint clean after --fix on 3 files (pre-existing import/order issue surfaced by AmortisationSchedule type rename, unrelated to this change's logic). Manually drove the running app (composer dev) with a scripted Playwright pass against /calculators/mortgage: opened the During-deal card's Payment Schedule accordion, confirmed monthly rows render first, clicked 'See Annual' and confirmed annual rows render (Year 1 balance £196,023 for the 5yr deal, matching hand-calculated remaining-balance math), confirmed the button label flips to 'See Monthly', and confirmed zero 'See Annual'/'See Monthly' buttons are present in the DOM while the accordion is collapsed.

Follow-up per user feedback: moved the annual/monthly toggle out of the collapsible body and into the header row itself, to the left of the chevron. Header restructured into two sibling CollapsibleTrigger elements (text label, flex-1; chevron-only, aria-labelled) around a controlled open/setOpen state, so the toggle button can sit between them as a plain sibling button and be conditionally rendered only while open — chevron rotation and rounded-bottom-corner-when-closed styling now driven off the open boolean instead of Radix's data-state CSS selectors. Re-verified with tsc, vitest (4/4), and a Playwright pass: 0 toggle buttons in DOM before opening, 1 after opening (positioned left of chevron per screenshot), view switches to annual rows on click, 0 again after re-closing via the chevron button.

Follow-up per user feedback (2): reverted the header-split approach and restored the original single-CollapsibleTrigger header with the toggle row below it inside CollapsibleContent (matches user's preferred layout). Fixed the visual seam the user flagged: the toggle row div now takes toneClasses[tone] (background + text color) so it reads as a visual continuation of the accordion header bar (brand-subtle bg/green text for During/Single cards, neutral grey bg/text for After-deal card) instead of a mismatched plain background with grey link text. Re-verified with tsc, vitest (4/4), and a Playwright screenshot across During-deal (brand tone) and After-deal (neutral tone) cards confirming the toggle row's background/text now match their respective header bars.

Follow-up per user feedback (3): user reverted back to the header-row placement (toggle left of chevron) but said it read as plain text, not a button. Restyled the toggle: rounded-md border border-current/30 px-2.5 py-1 hover:bg-current/10 transition-colors — using currentColor-based border/hover so it adapts to both the brand (green) and neutral (grey) header tones and both light/dark themes without new color tokens. Re-verified with tsc, vitest (4/4), and a Playwright screenshot + hover capture showing a visible bordered pill button with a tinted hover state.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
buildAmortisationSchedule now returns { monthly, annual } from a single pass (annual rows sum each year's interest/principal and take the balance at that year's final payment). PaymentScheduleSection takes a schedule prop, holds local monthly/annual view state, and renders a 'See Annual'/'See Monthly' toggle only inside the open CollapsibleContent. Single/During/After-deal cards and view-model.ts updated to the new AmortisationSchedule shape. Verified via tsc, vitest (4/4), eslint, and a Playwright-driven manual pass against the running app.
<!-- SECTION:FINAL_SUMMARY:END -->
