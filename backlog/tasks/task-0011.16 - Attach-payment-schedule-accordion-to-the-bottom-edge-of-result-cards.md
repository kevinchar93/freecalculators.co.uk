---
id: TASK-0011.16
title: Attach payment schedule accordion to the bottom edge of result cards
status: In Progress
assignee:
  - '@kevincharles018'
created_date: '2026-08-15 13:32'
updated_date: '2026-08-15 13:38'
labels: []
dependencies: []
parent_task_id: TASK-0011
ordinal: 34000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The payment schedule accordion currently floats as a bordered box inside the card's content padding. Redesign it to sit flush against the card's bottom edge and pick up the card's own corner radius, mirroring how the colored badge sits flush against the top edge — mocked up and approved at https://claude.ai/code/artifact/b26096de-bb32-4cb5-a498-7db8339ed5ed. Applies to the three cards that render PaymentScheduleSection: DuringDealCard, AfterDealCard, and SingleResultCard.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Payment schedule trigger spans the full width of the card, flush with its left/right/bottom edges, with no side padding gap.
- [x] #2 When collapsed, the trigger's bottom corners match the card's own corner radius, and the trigger label is fully visible (not clipped).
- [x] #3 When expanded, the scrollable table area (not the trigger) has the rounded bottom corners.
- [x] #4 Table column padding lines up with the padding of the rows above it in the card (loan amount, totals, etc).
- [x] #5 Table horizontal overflow, if any, is scoped to the table only and never causes the page or card to scroll horizontally.
- [x] #6 Applied consistently to DuringDealCard, AfterDealCard, and SingleResultCard.
- [x] #7 Existing accordion open/close behavior and sticky table header are preserved.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Redesign PaymentScheduleSection (payment-schedule-section.tsx): drop the bordered floating box; make trigger + table span full card width, trigger bg picks tone (brand for During/Your-results, neutral for After) via new tone prop, bottom corners rounded-b-2xl only when closed (rounded-b-2xl on trigger closed / on table wrapper when open), table cell padding aligned to card's px-5 content padding, table horizontal scroll scoped to its own wrapper (min-w-0 chain) so it never bleeds into the card/page.
2. Move <PaymentScheduleSection> out of the padded content div in DuringDealCard and AfterDealCard so it becomes a direct sibling of that div inside <Card>, flush with the card edges. Pass tone='brand' / tone='neutral' respectively.
3. Move <PaymentScheduleSection> out of both the p-5 wrapper and the flex flex-col gap-4 wrapper in SingleResultCard, to sibling of the p-5 div inside <Card>. Pass tone='brand'.
4. Manually verify in the browser (run dev server) that: label isn't clipped when collapsed, corners round correctly in both states, table column alignment matches the rows above, and no horizontal page/card scroll appears at mobile width.
5. Run existing tests (index.test.tsx) and typecheck/lint.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented: PaymentScheduleSection redesigned as a full-width Collapsible flush with card edges (tone prop: brand/neutral), rounded-b-2xl on trigger when closed and on table wrapper when open, table cell padding aligned to px-5 card content padding, min-w-0 chain to keep any overflow scoped to the table. Moved the component out of each card's padded content div (During/After-deal-card, SingleResultCard) so it sits as a direct sibling inside <Card>. Verified visually via dev server screenshots at desktop and mobile widths, in both open/closed states, in both light/dark-capable brand tones. Confirmed a pre-existing horizontal-overflow issue in the mortgage form/page at narrow widths is unrelated to this change (reproduced identically on unmodified main). types:check, lint (scoped to touched files), and index.test.tsx all pass.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Redesigned PaymentScheduleSection so it's a full-width accordion flush with the card's own left/right/bottom edges, rounded to match the card's corner radius when collapsed (mirroring the top badge) and rounded on the table wrapper instead when expanded. Table cell padding now lines up with px-5 card content padding, and overflow is scoped to a min-w-0 chain so it can never drag the card or page sideways. Applied to DuringDealCard (brand tone), AfterDealCard (neutral tone), and SingleResultCard (brand tone) by moving the component out of each card's padded content div to be a direct sibling inside <Card>. Verified by running the app (php artisan serve + vite) and taking headless-Chrome screenshots at desktop and mobile widths, in both open/closed states: label not clipped, corners round correctly, month/year column ('Oct '26' etc, from existing formatShortMonthYear) aligns with the £ rows above. types:check, lint on touched files, and index.test.tsx all pass.
<!-- SECTION:FINAL_SUMMARY:END -->
