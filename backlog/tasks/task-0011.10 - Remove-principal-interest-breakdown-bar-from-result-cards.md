---
id: TASK-0011.10
title: Remove principal/interest breakdown bar from result cards
status: In Progress
assignee:
  - '@claude'
created_date: '2026-08-15 09:27'
updated_date: '2026-08-15 09:28'
labels: []
dependencies: []
parent_task_id: TASK-0011
ordinal: 28000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The four result cards (Your Results, During-your-deal, After-your-deal, Summary) each render a PaymentBreakdownBar (the principal/interest split progress bar with percentage labels). Remove it from all four cards' rendered output while keeping the PaymentBreakdownBar component itself intact for potential future reuse.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 PaymentBreakdownBar is no longer rendered in single-result-card, during-deal-card, after-deal-card, or summary-card
- [ ] #2 payment-breakdown-bar.tsx component file and its exports remain unchanged
- [ ] #3 Any now-unused split-related props (showSplit, principalPercent, interestPercent, principalPercentLabel, interestPercentLabel) are removed from the four card components' prop interfaces and their call sites in results-panel.tsx
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Remove PaymentBreakdownBar import + JSX usage from single-result-card.tsx, during-deal-card.tsx, after-deal-card.tsx, summary-card.tsx, and drop showSplit/principalPercent/interestPercent/principalPercentLabel/interestPercentLabel from each card's props interface and function signature.
2. Remove the same 5 props being passed to each card in results-panel.tsx.
3. demo-data.ts: remove showSplit/principalPercent/interestPercent/principalPercentLabel/interestPercentLabel fields (and the ...xSplitRepay spreads) from all 8 exported demo objects; remove the now-unused splitPercentages() calls and its import (splitPercentages function itself stays in calculations.ts, PaymentBreakdownBar component file stays untouched).
4. Verify: npx vitest run, npm run types:check, Playwright visual check that no split bar renders anywhere and no console errors.
<!-- SECTION:PLAN:END -->
