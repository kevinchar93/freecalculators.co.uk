---
id: TASK-0011.10
title: Remove principal/interest breakdown bar from result cards
status: Done
assignee:
  - '@claude'
created_date: '2026-08-15 09:27'
updated_date: '2026-08-15 09:33'
labels: []
dependencies: []
parent_task_id: TASK-0011
ordinal: 28000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The four result cards (Your Results, During-your-deal, After-your-deal, Summary) each render a PaymentBreakdownBar (the principal/interest split progress bar with percentage labels). Remove it from all four cards' rendered output while keeping the PaymentBreakdownBar component itself intact for potential future reuse.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 PaymentBreakdownBar is no longer rendered in single-result-card, during-deal-card, after-deal-card, or summary-card
- [x] #2 payment-breakdown-bar.tsx component file and its exports remain unchanged
- [x] #3 Any now-unused split-related props (showSplit, principalPercent, interestPercent, principalPercentLabel, interestPercentLabel) are removed from the four card components' prop interfaces and their call sites in results-panel.tsx
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Remove PaymentBreakdownBar import + JSX usage from single-result-card.tsx, during-deal-card.tsx, after-deal-card.tsx, summary-card.tsx, and drop showSplit/principalPercent/interestPercent/principalPercentLabel/interestPercentLabel from each card's props interface and function signature.
2. Remove the same 5 props being passed to each card in results-panel.tsx.
3. demo-data.ts: remove showSplit/principalPercent/interestPercent/principalPercentLabel/interestPercentLabel fields (and the ...xSplitRepay spreads) from all 8 exported demo objects; remove the now-unused splitPercentages() calls and its import (splitPercentages function itself stays in calculations.ts, PaymentBreakdownBar component file stays untouched).
4. Verify: npx vitest run, npm run types:check, Playwright visual check that no split bar renders anywhere and no console errors.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Removed PaymentBreakdownBar rendering and its showSplit/principalPercent/interestPercent/principalPercentLabel/interestPercentLabel props from single-result-card.tsx, during-deal-card.tsx, after-deal-card.tsx, and summary-card.tsx (and their call sites in results-panel.tsx). demo-data.ts: dropped the same fields and the now-unused splitPercentages() calls from all 8 exported demo objects, plus the splitPercentages import. Left payment-breakdown-bar.tsx component and the splitPercentages function in calculations.ts untouched/exported for future reuse, per the request to keep the component. Verified: npx vitest run (4/4 passing), npm run types:check (clean), Playwright screenshots of single/during/after/summary in both repayment and interest-only states via composer run dev — no split bar renders anywhere, no console errors. Pre-existing eslint import/order warnings in these files predate this change (confirmed via git diff, lines untouched).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Removed the PaymentBreakdownBar (principal/interest split bar) from all four result cards (Your Results, During-your-deal, After-your-deal, Summary), dropping the now-unused split-related props from each card's interface and their demo-data/results-panel wiring. The PaymentBreakdownBar component file and the splitPercentages calculation function remain intact for future reuse. Verified with vitest, tsc, and Playwright screenshots of all card states (single, has-deal repayment, has-deal interest-only) — no split bar renders and no console errors.
<!-- SECTION:FINAL_SUMMARY:END -->
