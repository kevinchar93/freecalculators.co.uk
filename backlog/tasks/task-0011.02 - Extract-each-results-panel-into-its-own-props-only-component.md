---
id: TASK-0011.02
title: Extract each results panel into its own props-only component
status: In Progress
assignee:
  - '@claude'
created_date: '2026-08-13 07:27'
updated_date: '2026-08-15 00:32'
labels:
  - core-calculator
dependencies: []
parent_task_id: TASK-0011
ordinal: 4000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
results-panel.tsx is a single 328-line component that renders the single/during-deal/after-deal/summary cards inline and internally chooses demo-data objects. Split each card into its own component file that receives everything it renders via props and holds no data-selection logic, so each panel is easy to review in isolation.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Single-result, during-deal, after-deal and summary cards each live in their own component file
- [x] #2 Each panel component receives its display values via props and imports no demo-data directly
- [x] #3 ResultsPanel becomes a thin composition that decides which panels to show and passes their data down
- [x] #4 Rendered output is unchanged from the current page
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add SplitBreakdown shared prop fields (showSplit, principalPercent, interestPercent, principalPercentLabel, interestPercentLabel) reused via intersection across new card components.
2. Create components/single-result-card.tsx (SingleResultCard): props monthlyPayment, housePrice, deposit, depositPercentage, loan, totalInterest, totalPaid, dateLabel, endDate, showVehicleNotice, paymentSchedule + SplitBreakdown. No demo-data import.
3. Create components/during-deal-card.tsx (DuringDealCard): props monthlyPayment, totalPaidLabel, totalPaid, totalInterest, totalPrincipal, balanceAtEnd, endDate, paymentSchedule + SplitBreakdown.
4. Create components/after-deal-card.tsx (AfterDealCard): props monthlyPayment, changeText, totalPaidLabel, totalPaid, totalInterest, dateLabel, endDate, paymentSchedule + SplitBreakdown.
5. Create components/summary-card.tsx (SummaryCard): props housePrice, deposit, depositPercentage, loan, totalPaidLabel, totalPaid, totalInterest, showVehicleNotice + SplitBreakdown.
6. Update demo-data.ts: replace depositLine field with deposit + depositPercentage in singleCardRepayment, singleCardInterestOnly, summaryCardRepayment, summaryCardInterestOnly; remove now-unused DEMO_DEPOSIT_LINE.
7. Rewrite results-panel.tsx to compose the four new components, assigning props individually (no spread), keeping demo-data variant selection, frostedCardClass export, and the two ad Card blocks unchanged.
8. Verify: npx vitest run index.test.tsx, npm run types:check, visual check in dev server toggling Interest Only / Has Deal.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Created single-result-card.tsx, during-deal-card.tsx, after-deal-card.tsx, summary-card.tsx — each props-only, no demo-data import. Moved frostedCardClass to a new card-styles.ts to avoid a circular import (results-panel.tsx -> card components -> results-panel.tsx). Split demo-data.ts's combined depositLine string into separate deposit + depositPercentage fields so cards compose the "X (Y%)" line themselves from two value props instead of importing a pre-joined string. results-panel.tsx now only selects the demo-data variant (interest-only vs repayment) and wires named props to each card (no spreading), per review feedback. Verified: npx vitest run index.test.tsx (4/4 passing), npm run types:check (no new errors; remaining errors are pre-existing unrelated generated-route issues, including a pre-existing results.monthlyPaymentLowerLabel copy-key mismatch that predates this task). Also ran the app via composer run dev + a Playwright script and screenshotted all four card variants (single/during/after/summary, repayment and interest-only) plus the payment schedule expand — rendered output matches the pre-refactor page exactly, no console errors.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Split results-panel.tsx's four inline result cards into their own props-only components (single-result-card.tsx, during-deal-card.tsx, after-deal-card.tsx, summary-card.tsx). ResultsPanel is now a thin composition: it still picks the demo-data variant by mortgage type and passes named props down (no spreading), but holds no card markup. Verified with vitest (4/4 passing), tsc --noEmit (no new errors), and a Playwright-driven visual check of all card variants plus the payment schedule expand — output is pixel-identical to the pre-refactor page.
<!-- SECTION:FINAL_SUMMARY:END -->
